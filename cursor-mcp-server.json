{
  "mcpServers": {
    "spf-mcp": {
      "command": "npm",
      "args": ["run", "mcp"],
      "env": {
        "NODE_ENV": "production",
        "JWT_SECRET": "your-jwt-secret-here",
        "API_KEY": "your-api-key-here"
      }
    }
  }
}
